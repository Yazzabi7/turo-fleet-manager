{
  "name": "turo-fleet-manager",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend-new"
  ],
  "scripts": {
    "build": "cd frontend-new && npm install && npm run build"
  }
}
